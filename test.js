function calculateRandomNumber() {
    return Math.random();
}

// find the sum of two numbers
function sum(a, b) {
    return a + b;
}

// find word without test string
// and return the word
function findWord(word) {
    return word.replace("test", "");
}   // end of findWord      